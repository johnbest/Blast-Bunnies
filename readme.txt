This is a game written in javaScript.
With bombs.

It was written during snow code 2012.


Installation
------------

You might need to re-run npm install 
(the packages are currently included in version control)


Run
---

To run the application use the command
node server.js

The application runs on port 3000.

Currently every time a webpage loads it creates a logged in user.